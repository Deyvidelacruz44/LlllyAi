# Netlify configuration for Next.js
[build]
  command = "npm run build"
  publish = ".next"

# Plugin de Next.js para Netlify
[[plugins]]
  package = "@netlify/plugin-nextjs"

# Fix Windows backslash paths that cause octal escape errors in ESM
[[plugins]]
  package = "./netlify-plugins/fix-handler-paths"
